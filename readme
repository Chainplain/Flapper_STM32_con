1 建立一个采样类和采样loop（用task做）
2 采样类 封装：
    1 发送和接收的接口
        send(data): 发送采样数据
        receive(data): 接收采样配置信息
    2 数据采集的列表[指针形式]
        长度，采样时间，采样方式，数据类型
3 采样loop
    1 loop函数可以放在任何地方
        1 任务里
        2 while里
        3 中断里
    